import React from 'react';

// when returning html, you just need the ( )
export const SearchForm = () => (
    <form>
      <input type="text" placeholder="pokemon number" value="" name="" />
      <button>Search</button>
  </form>
)
